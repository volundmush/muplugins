[project]
name = "muplugins"
version = "0.1.0"
requires-python = ">=3.14"
dependencies = [
    "muforge",
    "orjson",
    "pydantic[email]",
    "pyjwt",
    "passlib",
    "argon2-cffi",
    "asyncpg",
    "lark",
    "psutil"
]
